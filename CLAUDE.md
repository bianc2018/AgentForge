# CLAUDE.md

此文件为 Claude Code (claude.ai/code) 在此仓库中处理代码时提供指导。

## 语言

读取 `docs/sdd/sdd-config.md` 并使用 `language` 字段进行所有通信和文档生成。如果文件不存在，则使用项目上下文中最自然的语言。

切勿翻译 IT 技术术语 — 无论配置何种语言，它们始终保留英文：API, backend, frontend, endpoint, deploy, branch, commit, pull request, merge, cache, token, bug, framework, pipeline, build, release, feature, sprint, backlog, mock, stub, refactor, hotfix, rollback, CI/CD, log, test, debug。

